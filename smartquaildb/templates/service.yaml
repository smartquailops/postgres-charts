apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartquaildb.fullname" . }}
  labels:
    {{- include "smartquaildb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartquaildb.selectorLabels" . | nindent 4 }}
